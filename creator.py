import tkinter as tk
import pygame
from tkinter import messagebox
from utils import resource_path

class QuizCreator:
    def __init__(self, parent):
        self.parent = parent
        self.root = tk.Toplevel(parent)
        # ... (rest of your QuizCreator __init__ code, including sound setup, UI, etc.)

    # ... (all methods from QuizCreator)
